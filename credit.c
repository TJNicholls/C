//include headers
#include <stdio.h>
#include <math.h>
#include <cs50.h>

//main function
int main(void)
{
    // declare variables. long used for a longer float
    long card;
    string A, M, V, I
    int length;

    do
    {
    card = get_long("Please enter your card number: ");
    }
    while (card < 3300000000000)

    if (length == 13 || length ==15 length == 16)

    else
    {
        printf("INVALID"\n);
    }

   printf("%s\n"____);
}